---
title: 藍鳥推特
date: 2025-08-26 13:15:55
tags:
---

cloudflare D1 workers prism 
先設定了cloudflare D1
### 抓取資料



### 資料庫
test用的設定 大多取自原本下載時就有自帶的欄位
```
CREATE TABLE test_tweets(
tweet_id TEXT PRIMARY KEY,
text TEXT NOT NULL,
author_id TEXT NOT NULL,
type TEXT,
created_at TEXT,
hashtags TEXT,
urls TEXT,
media_type TEXT,
media_urls TEXT
);
```
需要保留的應該只有tweet_id(主鍵)
text,author_id(外鍵),排序用的created_at


### 後端

### 前端

寫前端囉(開心)  
Step 1 萬物之初
```
npm create vite@latest
```

### 重構

木冬幫我完成了大部份前端頁面，但因為全部擠在app.vue裡面，所以決定開始重構

## step.1 
首先分出ui類的組件
### loading 

因為只有一個boolean值，就不使用`props`控制了
使用v-if(至於沒有使用v-show的原因請見vue又騙我學新前端)  
將template,css都搬到都搬到loader之後基本上解決了。但在edge開啟時，主頁面尺寸較大，吃掉右側時間列

//todo: 將css更改為巢狀結構，目前更改時出現loading圖標過大的問題，應該是跟某些設定衝突


